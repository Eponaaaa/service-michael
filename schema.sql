CREATE DATABASE gallery;

USE gallery;

CREATE TABLE photos (
  id int NOT NULL AUTO_INCREMENT,
  url1 varchar(255),
  url2 varchar(255),
  url3 varchar(255),
  url4 varchar(255),
  url5 varchar(255),
  url6 varchar(255),
  url7 varchar(255),
  url8 varchar(255),
  url9 varchar(255),
  url10 varchar(255),
  PRIMARY KEY (id)
);